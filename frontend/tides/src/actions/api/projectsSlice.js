import { createSlice } from '@reduxjs/toolkit'

const projectsSlice = createSlice({
    name: 'projects',
    initialState: [],
    reducers: {
        projectFetch(state,action) {
            state.push({
                
            })
        }
    }

})